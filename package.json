{
  "name": "secureauth-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:tunnel": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"node start_tunnel.js\"",
    "tunnel": "node start_tunnel.js"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "vite": "^8.0.14"
  }
}
